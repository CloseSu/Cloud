spring:
  application:
    name: configclient
  cloud:
    config:
      profile: pro
      label: master
      discovery:
        enabled: true
        serviceId: configServer
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9876/eureka/